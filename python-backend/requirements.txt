# Core - Flask & API
flask==2.3.3
flask-cors==4.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Image processing
pillow==10.0.1
numpy==1.24.3
opencv-python==4.8.0.76

# Auth & Database
bcrypt==4.0.1
PyJWT==2.8.0
mysql-connector-python==8.1.0

# Utilities
requests==2.31.0

# Optional - AI model (requires Rust if building from source)
# transformers, torch, torchvision - uncomment when needed
# pip install transformers torch torchvision